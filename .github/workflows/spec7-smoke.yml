name: Spec 7 Smoke (expected to fail)
on:
  pull_request:
    paths:
      - '.github/workflows/spec7-smoke.yml'
jobs:
  unpinned:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
